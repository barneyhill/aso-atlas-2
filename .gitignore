# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/

# uv.lock and .python-version are committed for reproducible releases.

# Local secrets (RUNPOD_API_KEY, etc.)
.env

# Claude Code
CLAUDE.md
.claude/worktrees/

# Tests (local only), except the versioned dataset-release contract.
tests/*
!tests/test_release_splits.py

# macOS
.DS_Store

# Archive (old experiments, legacy code, reference PDFs)
archive/
analyses/hypotheses/
data/RNAdecayCafe_v1_onetable.csv.gz

# Large benchmark results (regenerated by just oligogym)
data/results/oligogym_benchmark.json

# Large per-fold prediction CSVs (regenerated by OligoAI eval)
data/results/oligoai/

# Large data files (regenerated by pipeline)
data/oligostack/raw/
data/models/
data/reference/
data/tmp/
data/md_metrics.csv
data/CCLE_celllines.csv

# Training artifacts
lightning_logs/
catboost_info/

# RunPod (infrastructure for remote training, not part of the paper)
runpod/

# HuggingFace release staging (regenerated by scripts/prepare_hf_release.py)
aso-atlas-2-release/*.parquet
aso-atlas-2-release/scn2a_all_data.csv

# Separate studies and local scratch, not part of this paper
off-target-investigation/
latex_dummy/
checkpoints/

# Audit intermediates: regenerated by `just audit`, too large to version.
# The curated ledgers under data/validation/*.csv are committed; these are not.
data/validation/dedup_lineage/

# Derived training snapshots (regenerated by the pipeline)
data/oligostack/processed/
