[project]
name = "patent-collate"
version = "0.1.0"
description = "LLM-powered pipeline for extracting structured data from USPTO patent XML tables"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "diskcache>=5.6.3",
    "openai>=2.6.1",
    "pydantic>=2.0",
    "pyyaml>=6.0.3",
    "tqdm>=4.60.0",
]
